{
  "name": "watechtask",
  "version": "1.0.0",
  "scripts": {
    "run:current": "cucumber-js test/**/*.feature --tags @Current -f json:report/cucumber_report.json",
    "test": "cross-env NODE_ENV=test jest",
    "run:all": "cross-env NODE_ENV=test npm run test",
    "run:chromium": "cross-env BROWSER=chromium jest",
    "run:firefox": "cross-env BROWSER=firefox jest",
    "run:webkit": "cross-env BROWSER=webkit jest",
    "generate:report": "cross-env NODE_ENV=test node reporter.ts"
  },
  "keywords": [],
  "author": "Eugene Chirkun",
  "license": "ISC",
  "description": "Test project. LVBET.COM test automation in training purposes.",
  "devDependencies": {
    "@cucumber/cucumber": "^10.8.0",
    "@playwright/test": "^1.45.1",
    "@types/node": "^20.14.10",
    "cross-env": "^7.0.3",
    "cucumber-html-reporter": "7.1.1",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "dotenv": "16.3.1",
    "ts-node": "10.9.1",
    "unique-names-generator": "4.7.1"
  }
}
